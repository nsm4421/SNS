name: sns
description: "sns project"
publish_to: 'none'
version: 1.0.0+1
environment:
  sdk: ^3.8.1
dependencies:
  flutter:
    sdk: flutter
  supabase_datasource:
    path: packages/supabase_datasource
  local_storage_datasource:
    path: packages/local_storage_datasource
  response_wrapper:
    path: packages/response_wrapper
  get_it: ^8.0.3
  injectable: ^2.5.0
  either_dart: ^1.0.0
  flutter_bloc: ^9.1.0
  logger: ^2.6.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
  build_runner: ^2.5.4
  injectable_generator: ^2.6.2
  json_annotation: ^4.9.0
  json_serializable: ^6.8.0
  copy_with_extension_gen: ^6.0.1
  bloc_test: ^10.0.0
  mocktail: ^1.0.4
  melos: ^3.0.0

flutter:
  uses-material-design: true
